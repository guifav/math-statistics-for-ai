# Requirements para GPU (CUDA 12.1)
# Instale com: pip install -r requirements.txt && pip install -r requirements-gpu.txt
# Ou diretamente: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
torchvision>=0.15.0
